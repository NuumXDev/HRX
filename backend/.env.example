# Google Gemini API key (Required for candidate resume and tech assessment AI analysis)
GEMINI_API_KEY=your_gemini_api_key_here

# SerpAPI key (Required for social profile lookup and background reference checks)
SERPAPI_KEY=your_serpapi_key_here

# Cloud database URL (Optional, defaults to local SQLite hrx.db if omitted)
# DATABASE_URL=postgresql://user:password@host:port/dbname
